{
    "apiVersion": "v1",
    "kind": "Pod",
    "metadata": {
        "name": "iscsi",
        "labels": {
            "name": "iscsi"
        }
    },
    "spec": {
        "containers": [{
            "name": "myfrontend",
            "image": "aosqe/storage",
            "imagePullPolicy": "IfNotPresent",
            "securityContext": {
                "privileged": true
            },
            "volumeMounts": [{
                "mountPath": "/mnt/iscsi",
                "name": "iscsi"
            }]
        }],
        "volumes": [{
            "name": "iscsi",
            "persistentVolumeClaim": {
                "claimName": "iscsic"
            }
        }]
    }
}
