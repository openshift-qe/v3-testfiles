spec:
  strategy:
    type: Recreate
    recreateParams:
      pre:
        execNewPod: ~
        tagImages:
        - containerName: ruby-helloworld-database
          to:
            apiVersion: v1
            kind: ImageStreamTag
            name: myis:tag1
        failurePolicy: Abort
