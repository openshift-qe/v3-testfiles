{
  "apiVersion":"v1",
  "kind": "Pod",
  "metadata": {
    "name": "hostname-pod",
    "labels": {
      "name": "hostname-pod"
    }
  },
  "spec": {
    "containers": [{
      "name": "hostname-pod",
      "image": "openshift/hello-openshift"
    }],
    "host" : "minion3.cluster.local"
  }
}

